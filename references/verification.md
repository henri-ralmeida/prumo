# Verification / Verificação

Observed checks, not a guarantee that every model, client configuration or operating system behaves identically.

Release candidate end-to-end checks can be repeated with `npm pack`, `node scripts/package-smoke.mjs`
and `node scripts/published-update-smoke.mjs`. The published-version matrix runs every Prumo version
available on npm against a local registry serving the candidate package. It preserves completed/skipped
tasks and exercises discussing, planning, running, reviewing, blocked, failed and validated continuation.
`node --test test/autostart-process.test.mjs` uses a real Windows dashboard process and the generated Startup VBS
in an isolated profile and port. Scheduler denial is simulated; the VBS is executed directly, without rebooting
or logging out. This does not claim that a real Windows login or native agent session has been tested.

Verificações observadas, não uma garantia de comportamento idêntico de todos os modelos, configurações de cliente ou sistemas operacionais.

| Layer / Camada | Evidence / Evidência |
|---|---|
| Current automated suite / Suíte automatizada atual | Engine, installer, update, autostart, server and dashboard checks run in CI; tests cover cumulative update notes, verified marker recovery, downgrade refusal, transactional central migration and legacy dashboard adoption / Testes de motor, instalador, atualização, inicialização automática, servidor e dashboard rodam no CI; testes cobrem notas cumulativas, recuperação conferida de marcadores, recusa de downgrade, migração central transacional e adoção do dashboard legado |
| Phase discussion and planning / Discussão e planejamento por fase | Engine lifecycle tests cover a persisted discussion before questions, current round receipts, one phase planner, atomic immutable task-plan batches, stale context, selective invalidation, legacy adoption and retry reuse / Testes do fluxo cobrem discussão persistida antes das perguntas, recibos da rodada atual, um planejador por fase, lote atômico de planos imutáveis, contexto obsoleto, invalidação seletiva, adoção legada e reutilização no retry |
| Later-phase inputs / Entradas de fase posterior | Tests require a current independently reviewed validation receipt, or an explicit skip waiver, before execution; changing that receipt invalidates review and completion / Testes exigem recibo atual de validação com revisão independente, ou dispensa explícita por tarefa pulada, antes da execução; alterar o recibo invalida revisão e conclusão |
| Behavioral gate / Aprovação funcional | Lint-only rejection, real code execution, justified documentation inspection, obsolete receipts and independent review / Recusa de lint isolado, execução real, inspeção justificada de documentação, recibos obsoletos e revisão independente |
| Global install and recovery / Instalação global e recuperação | Isolated profiles cover global versus local postinstall, detected harnesses, partial success with nonzero exit, idempotent repair, disabled scripts, dry-run, update and preserved opt-out / Perfis isolados cobrem postinstall global versus local, ambientes detectados, sucesso parcial com retorno não zero, reparo idempotente, scripts desabilitados, dry-run, atualização e opt-out preservado |
| Autostart / Inicialização automática | Simulated Windows Task Scheduler, macOS LaunchAgent, Linux user systemd and XDG fallback checks cover paths with spaces, enable, status, restart and removal without changing the test machine / Simulações do Agendador de Tarefas, LaunchAgent, systemd do usuário e fallback XDG cobrem caminhos com espaços, habilitação, status, reinício e remoção sem alterar a máquina de testes |
| Global read-only server / Servidor global somente leitura | Fixtures cover no runs, one run, multiple known workspaces, registered projects, dynamic discovery, safe paths and `/api/health`; the global service does not use `--sync-plan` / Fixtures cobrem nenhuma run, uma run, vários workspaces conhecidos, projetos registrados, descoberta dinâmica, caminhos seguros e `/api/health`; o serviço global não usa `--sync-plan` |
| Dashboard / Dashboard | Browser-oriented tests cover status filters, direct dependency context, 6/48/206-task density, responsive wrapping, fixed arrowheads, reduced motion, sidebar order and collapse, English and PT-BR / Testes orientados ao navegador cobrem filtros, contexto direto de dependências, densidade com 6/48/206 tarefas, quebra responsiva, pontas de seta fixas, movimento reduzido, ordem e recolhimento lateral, inglês e PT-BR |
| Packaged npm / Pacote npm | `scripts/package-smoke.mjs` uses isolated npm prefixes and registries to verify the packed CLI, four complete adapters, update and registry failure; it does not alter real harness profiles / O script usa prefixos e registros npm isolados para verificar CLI empacotada, quatro adaptadores completos, atualização e falha de registro; ele não altera perfis reais |

The [CI matrix](../.github/workflows/ci.yml) runs supported automated checks on Windows, macOS and Linux with Node.js 22 and 24. Check the [Actions run for the release commit](https://github.com/henri-ralmeida/prumo/actions) and release notes for the exact result; platform-specific tests may be skipped elsewhere. Automated fixtures do not prove a real login startup, a real Claude Code/Kiro/Codex/DSH session, native question UI availability or model behavior. For DSH, fixture installation, `doctor --dsh` and `dsh --profile headless --dump-config` establish structural configuration only: they do not prove provider credentials, a real model conversation, model behavior or upstream stability. The source validated for this adapter was `@deepseek-ai/dsh` `0.1.6-alpha.2`, still alpha/developer preview. Verify broader claims on the target machine and harness before reporting them as observed.

A [matriz de CI](../.github/workflows/ci.yml) executa os testes automatizados compatíveis no Windows, macOS e Linux com Node.js 22 e 24. Confira o [Actions do commit da release](https://github.com/henri-ralmeida/prumo/actions) e as notas para o resultado exato; testes específicos de uma plataforma podem ser ignorados nas demais. Fixtures automatizadas não comprovam inicialização em um login real, uma sessão real no Claude Code/Kiro/Codex/DSH, disponibilidade da interface nativa de perguntas ou comportamento do modelo. No DSH, instalação em fixture, `doctor --dsh` e `dsh --profile headless --dump-config` comprovam somente configuração estrutural: não comprovam credencial de provedor, conversa real com modelo, comportamento do modelo ou estabilidade upstream. A fonte validada para este adaptador foi `@deepseek-ai/dsh` `0.1.6-alpha.2`, ainda em alpha/developer preview. Verifique afirmações mais amplas na máquina e no ambiente alvo antes de relatá-las como observadas.

The current version preserves graph-foreman durable state, older task-scoped runs and their history. Installation migrates graph state, saved graph backups and top-level plan or handoff files transactionally; generated execution directories, dependency copies and build outputs are discarded after verification, while project-local runs stay in place. The skill normalizes nonterminal legacy contracts before synchronization without widening the requested planning scope. New phased runs use one principal-conversation discussion and one read-only planner per phase. The user may plan independent phases in parallel; a phase waits when any unfinished member has an unfinished external dependency. When the user explicitly selects one existing task, it keeps one discussion and one planner; internal deliverables remain inside that task plan unless the user approves a graph expansion. Corrective review can reuse deterministic static step receipts only when declared paths remain unchanged; functional checks always rerun. The engine verifies record structure, freshness and receipts. The orchestrator, planner, executor and independent reviewer remain responsible for semantic quality and real dispatch.

A versão atual preserva os dados duráveis do graph-foreman, runs antigas no modo por tarefa e seu histórico. A instalação migra de forma transacional estado do grafo, backups salvos e arquivos de plano ou handoff no topo; diretórios gerados de execução, cópias de dependências e saídas de build são descartados depois da verificação, enquanto runs dentro de projetos ficam no lugar. A skill normaliza contratos legados não terminais antes da sincronização sem ampliar o escopo solicitado. Runs novas usam uma discussão na conversa principal e um planejador somente leitura por fase. O usuário pode planejar fases independentes em paralelo; uma fase espera quando qualquer membro não concluído possui dependência externa ainda aberta. Quando o usuário escolhe explicitamente uma tarefa existente, ela mantém uma discussão e um planner; entregas internas permanecem nesse plano salvo aprovação para ampliar o grafo. A revisão corretiva pode reutilizar recibos de etapas estáticas determinísticas somente quando os caminhos declarados continuam iguais; verificações funcionais sempre executam novamente. O motor verifica estrutura, atualidade e recibos. Orquestrador, planejador, executor e revisor independente continuam responsáveis pela qualidade semântica e pelo despacho real.
